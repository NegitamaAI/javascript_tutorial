# Exercise: Variables, Hoisting, and Type Checks

## Tasks
- [ ] Fix TDZ errors by reordering code.
- [ ] Make a function `safeGet(obj, path)` that returns `undefined` if any segment is missing.
- [ ] Extend `typeOf` to distinguish between plain objects and class instances (hint: `Object.getPrototypeOf`).

## Starter
```javascript
// Add your code here
```
