// Example test template (Node >=18 recommended)
import assert from 'node:assert/strict';

// Replace with real function import
function add(a, b) { return a + b; }

assert.equal(add(2, 3), 5);
console.log('✅ Example test passed');
